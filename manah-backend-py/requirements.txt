# Manah Backend — Python/FastAPI
# Python 3.11+

# === Web Framework ===
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# === Database (async SQLite) ===
aiosqlite>=0.20.0

# === Auth / Security ===
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0
httpx>=0.27.0          # For JWKS fetch (Supabase RS256)

# === LLM (OpenAI-compatible: OpenRouter, Qubrid) ===
openai>=1.50.0

# === SSE Streaming ===
sse-starlette>=2.0.0

# === Rate Limiting ===
slowapi>=0.1.9

# === Settings / Config ===
pydantic[email]>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0

# === Background Scheduler ===
apscheduler>=3.10.0

# === Utilities ===
python-multipart>=0.0.9  # Form data support
loguru>=0.7.0

# === Testing ===
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0            # Also used as async test client
